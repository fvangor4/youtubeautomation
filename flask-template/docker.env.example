# Copy this file to docker.env and fill in real values before running the container.
YOUTUBE_API_KEY=replace-with-your-youtube-api-key
# Optional: protect save/share/archive endpoints with a token
APP_AUTH_TOKEN=choose-a-token
# Optional: enable Discord sharing of snapshots
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/replace-me
